import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    display: 'flex',
  },
  text: {
    fontSize: 100,
    fontWeight: 'bold',
    textAlign: 'center',
    marginTop: 'auto',
    marginRight: 'auto',
    marginBottom: 'auto',
    marginLeft: 'auto',
  },
});

export default styles;
